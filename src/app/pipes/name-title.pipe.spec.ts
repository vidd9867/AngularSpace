import { NameTitlePipe } from './name-title.pipe';

describe('NameTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new NameTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
